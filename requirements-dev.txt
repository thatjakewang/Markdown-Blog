# Development/test top-level dependencies (on top of requirements.txt).
# The fully pinned set lives in requirements-dev.lock — install that locally and
# in CI so tests run against the exact versions requirements.lock deploys.
# Regenerate both, in this order, after changing any pin here or in requirements.txt:
#    uv pip compile requirements.txt -o requirements.lock
#    uv pip compile requirements-dev.txt -c requirements.lock -o requirements-dev.lock
# The -c flag constrains shared packages to the deployed versions, so the only
# difference between the two locks is the test-only tree (pytest, httpx).
-r requirements.txt
pytest==9.0.3
httpx==0.28.1
